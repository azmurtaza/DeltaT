<Window x:Class="Kelvin.App.Views.FingerprintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Kelvin.App.Controls"
        Title="Kelvin — thermal fingerprint" Width="520" Height="560"
        Background="{StaticResource Brush.Bg}" Foreground="{StaticResource Brush.Text}"
        FontFamily="{StaticResource Font.Ui}" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" UseLayoutRounding="True">
    <Grid Margin="28,22">

        <!-- ============ intro ============ -->
        <StackPanel Visibility="{Binding State, Converter={StaticResource StateToVis}, ConverterParameter=intro}">
            <TextBlock Style="{StaticResource Text.Caption}" Text="THERMAL FINGERPRINT"/>
            <TextBlock Style="{StaticResource Text.H1}" Text="A 3-minute honest look" Margin="0,8,0,10"/>
            <TextBlock Style="{StaticResource Text.Body}" FontSize="12.5" LineHeight="19" TextWrapping="Wrap">
                Kelvin idles for 25 seconds, then loads every CPU core for 2½ minutes and watches:
                how fast the heat soaks in, where it settles, and whether the silicon has to throttle.
                Same test, same numbers — run it monthly and the drift <Run FontStyle="Italic">is</Run> your paste aging.
            </TextBlock>
            <TextBlock Style="{StaticResource Text.Dim}" FontSize="12" LineHeight="18" Margin="0,14,0,0" TextWrapping="Wrap">
                • Plug in the charger — battery runs aren't comparable.<LineBreak/>
                • Close heavy apps so the idle phase is actually idle.<LineBreak/>
                • Fans will spin up. That's the point.
            </TextBlock>
            <Border Background="#1A1206" BorderBrush="#3A2A0A" BorderThickness="1" CornerRadius="8"
                    Padding="12,9" Margin="0,16,0,0"
                    Visibility="{Binding OnBattery, Converter={StaticResource BoolToVis}}">
                <TextBlock FontSize="12" Foreground="{StaticResource Brush.Warm}"
                           Text="You're on battery right now — results will be recorded but flagged."/>
            </Border>
            <Button Style="{StaticResource Button.Primary}" Content="Start the test" Padding="20,10"
                    FontSize="13.5" HorizontalAlignment="Left" Margin="0,22,0,0"
                    Command="{Binding StartCommand}"/>
        </StackPanel>

        <!-- ============ running ============ -->
        <Grid Visibility="{Binding State, Converter={StaticResource StateToVis}, ConverterParameter=running}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock Style="{StaticResource Text.Caption}" Text="FINGERPRINT IN PROGRESS"/>
                <TextBlock Style="{StaticResource Text.H1}" FontSize="18" Text="{Binding Phase}" Margin="0,8,0,0"/>
                <TextBlock Style="{StaticResource Text.Dim}" FontSize="12" Margin="0,4,0,0"
                           Text="{Binding SecondsLeft, StringFormat='{}{0:0} s remaining in this phase'}"/>
            </StackPanel>
            <controls:RingGauge Grid.Row="1" Width="200" Height="200"
                                Value="{Binding CurrentTemp}" Maximum="100"
                                HasValue="{Binding HasCurrentTemp}" Unit="°C" SubText="CPU"/>
            <Button Grid.Row="2" Style="{StaticResource Button.Ghost}" Content="Stop the test"
                    HorizontalAlignment="Center" Foreground="{StaticResource Brush.Hot}"
                    Command="{Binding StopCommand}"/>
        </Grid>

        <!-- ============ done ============ -->
        <StackPanel Visibility="{Binding State, Converter={StaticResource StateToVis}, ConverterParameter=done}">
            <TextBlock Style="{StaticResource Text.Caption}" Text="FINGERPRINT RESULT"/>
            <ItemsControl ItemsSource="{Binding ResultCells}" Margin="-5,14,-5,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Margin="5" Padding="12,10">
                            <StackPanel>
                                <TextBlock Style="{StaticResource Text.Caption}" Text="{Binding Label}"/>
                                <TextBlock Style="{StaticResource Text.Numeral}" FontSize="19" Margin="0,4,0,0"
                                           Text="{Binding Value}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Border Style="{StaticResource Card}" Margin="0,12,0,0" Padding="16,13">
                <TextBlock Style="{StaticResource Text.Body}" FontSize="12.5" LineHeight="19"
                           Text="{Binding VerdictText}" TextWrapping="Wrap"/>
            </Border>
            <Button Style="{StaticResource Button.Primary}" Content="Done" Padding="20,9"
                    HorizontalAlignment="Left" Margin="0,18,0,0" Click="OnDone"/>
        </StackPanel>
    </Grid>
</Window>
